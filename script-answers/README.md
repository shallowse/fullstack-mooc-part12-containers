The `.md` files contain a readable representation of the exercises.

The formatting produced by `script(1)` in the *.txt files is not as readable
as the formatting produced by using `$cat filename.txt > filename.txt.md`.